{
  "name": "server",
  "version": "1.0.0",
  "description": "mock server for e-commerce with micro frontends",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 4000 --routes routes.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
